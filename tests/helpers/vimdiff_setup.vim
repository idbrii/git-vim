" Doesn't do anything special... just use the global setup
source helpers/setup.vim

" Append another line to the first file so you'll be able to diff it
silent ! echo appended another line >> first_file.txt;
